import XCTest
@testable import SwiftUIBestPractices

final class SwiftUIBestPracticesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUIBestPractices().text, "Hello, World!")
    }
}
